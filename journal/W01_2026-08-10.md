# Tuần 1 — Toán nền tảng + PyTorch (chỉ học những gì cần)

- **Tuần lịch:** 2026-08-10 → 2026-08-16
- **Mục tiêu tuần (copy từ [Week-01/README.md](../Week-01/README.md)):**
  - [ ] Ôn linear algebra (matrix multiply, dot product, shape/broadcasting)
  - [ ] Ôn calculus: gradient, chain rule
  - [ ] Ôn probability: softmax, cross-entropy
  - [ ] Thành thạo PyTorch: tensor, autograd, `nn.Module`, optimizer, training loop
  - [x] Xác nhận GPU chạy được (CUDA trên RTX 3070 Ti hoặc Mac MPS)
- **Ngân sách thời gian dự kiến:** ~10–12 giờ | **Thực tế cộng dồn:** [Chưa xác minh] — chưa ghi thời lượng buổi 11/08

---

## Log theo buổi

### 2026-08-11 (thời lượng: [Chưa xác minh]) — Kiểm tra môi trường PyTorch/GPU

**Đã làm:**
- Chạy `Week-01/check_gpu.py` trên máy Windows + RTX 3070 Ti. Script báo CUDA khả dụng và phép nhân ma trận 1000×1000 chạy được trên `cuda`.

**Bằng chứng (evidence):**

| Loại | Chi tiết |
|------|----------|
| Lệnh + output | `python Week-01/check_gpu.py` → `[OK] CUDA khả dụng`, `Kết quả shape: (1000, 1000) \| mean=-0.0026`. Log nguyên văn: [evidence/W01/check_gpu_2026-08-11.log](evidence/W01/check_gpu_2026-08-11.log) |
| Số liệu (đo bằng chính script, ngày 2026-08-11) | OS = `Windows-10-10.0.26200-SP0`; PyTorch = `2.5.1+cu121`; GPU = `NVIDIA GeForce RTX 3070 Ti`; VRAM tổng = `8.0 GB`; CUDA version (theo `torch.version.cuda`) = `12.1` |
| Commit | [Chưa xác minh] — chưa commit tại thời điểm ghi nhật ký |

**Vướng mắc / lỗi gặp:**
- Không có lỗi khi chạy script.
- **Điểm không khớp cần lưu:** trước buổi này, checklist trong [Week-01/README.md](../Week-01/README.md) (dòng 42) đã tick mục `check_gpu.py` với ghi chú "✅ MPS khả dụng — macOS arm64, torch 2.12.1". Lần chạy ngày 2026-08-11 là **Windows + CUDA + torch 2.5.1+cu121**, không phải macOS/MPS. Tôi không kiểm chứng được lần chạy macOS đó (không có log kèm theo trong repo), nên giữ nguyên ghi chú cũ và ghi thêm lần chạy đã có bằng chứng này bên cạnh.

**Học được:**
- `torch.version.cuda` là phiên bản CUDA runtime mà bản PyTorch được build cùng (ở đây 12.1), không nhất thiết trùng phiên bản driver CUDA của máy.
- VRAM 8 GB là ràng buộc cứng cần nhớ cho các tuần sau (pretraining / fine-tune) — script đã in ra để đối chiếu về sau.

**Việc tiếp theo:**
- Đọc `Week-01/02_theory_notes.md` (thêm ngày 2026-08-11), chạy lại các snippet.
- Tự code `Week-01/05_train_mlp.py` (MLP + training loop trên toy dataset), ghi lại loss đầu/cuối làm số liệu tuần.
- Bắt đầu `Week-01/03_math_cheat_sheet.md`.

**Ghi chú cấu trúc (2026-08-11):** các file Week-01 được đổi tên thêm tiền tố số thứ tự học (`check_gpu.py` → `01_check_gpu.py`, ...); `quiz.md`/`quiz_solution.md` giữ nguyên tên vì do `scripts/generate_quiz.py` sinh ra. Các dòng evidence phía trên giữ tên cũ vì đó là lệnh đã chạy trước khi đổi tên.

---

## Tổng kết cuối tuần

*Chưa tổng kết — tuần đang diễn ra (ghi ngày 2026-08-11).*

### Deliverable đối chiếu checklist [Week-01/README.md](../Week-01/README.md)

| Deliverable | Trạng thái | Bằng chứng |
|-------------|-----------|------------|
| Xác nhận GPU hoạt động (`01_check_gpu.py`, tên cũ `check_gpu.py`) | ✅ Done | [evidence/W01/check_gpu_2026-08-11.log](evidence/W01/check_gpu_2026-08-11.log) — chạy 2026-08-11, CUDA khả dụng trên RTX 3070 Ti |
| `05_train_mlp.py` train được MLP, loss giảm | ⏳ Dở | chưa có |
| `03_math_cheat_sheet.md` viết bằng lời mình | ⏳ Dở | chưa có |

### Số liệu chính của tuần

| Metric | Baseline | Sau khi làm | Cách đo | Ngày đo |
|--------|----------|-------------|---------|---------|
| VRAM khả dụng của GPU | — | 8.0 GB | `torch.cuda.get_device_properties(0).total_memory` trong `Week-01/check_gpu.py` | 2026-08-11 |

### Quiz tuần

- Chưa làm `Week-01/quiz.md`.

### Nhìn lại (retrospective ngắn)

- Chưa viết — để lại cuối tuần.
